package snailmail.client

import snailmail.core.api.API

fun main() {
    /*
    val ClientAPI = API()
    val clientSession = ClientSession(ClientAPI)
    clientSession.startSession()
    var cmd = readLine()!!
    while (cmd.toLowerCase() != "quit") {
        clientSession.doCommand(cmd)
        cmd = readLine()!!
    }
    clientSession.endSession()
    */
}